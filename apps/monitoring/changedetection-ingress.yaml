apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: changedetection
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure-int
spec:
  ingressClassName: traefik
  rules:
    - host: change.romanpeters.nl
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: changedetection
                port:
                  name: http

apiVersion: v1
kind: Service
metadata:
  name: changedetection
  labels:
    app.kubernetes.io/name: changedetection
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: changedetection
  ports:
    - name: http
      port: 5000
      targetPort: http

apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: tautulli-xscheme
  namespace: media
spec:
  headers:
    customRequestHeaders:
      X-Scheme: "https"
      X-Forwarded-Proto: "https"
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tautulli
  namespace: media
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
    traefik.ingress.kubernetes.io/router.middlewares: media-tautulli-xscheme@kubernetescrd
spec:
  ingressClassName: traefik
  rules:
    - host: tautulli.romanpeters.nl
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: tautulli
                port:
                  name: http

apiVersion: v1
kind: Service
metadata:
  name: homepage-tv
  labels:
    app.kubernetes.io/name: homepage-tv
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: homepage-tv
  ports:
    - name: http
      port: 3000
      targetPort: http
